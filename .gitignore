# Rust build artifacts
pgp-mobile/target/
target/

# UniFFI generated bindings are tracked, but the temporary headers staging
# directory created by build-xcframework.sh is not.
bindings/headers/
pgp-mobile/bindings/

# macOS
.DS_Store

# Xcode
**/xcuserdata/
DerivedData/
build/

# Swift Package Manager
.build/
Packages/

# XCFramework (built artifact, not source)
*.xcframework/
PgpMobile.arm64e-build-manifest.json
SQLCipher.arm64e-build-manifest.json
SQLCipher-PrivacyInfo.xcprivacy
SQLCipher.xcframework.sha256
SQLCipher.xcframework.zip
SQLCipher.xcframework.release.json

# App Store candidate gate verdict, written by ci_scripts/ci_post_clone.sh
# (WF2) for the archive scheme pre-action; only ever exists on a CI machine
.app-store-candidate-verdict.json

# Claude Code local/personal config (not shared project config)
.claude/worktrees/
.claude/settings.local.json
CLAUDE.local.md

# Local Codex/agent helper config
.codex/
.agents/

# Python bytecode caches
__pycache__/
*.pyc
