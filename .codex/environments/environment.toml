# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "cypherair-main"

[setup]
script = ""

[setup.darwin]
script = '''
set -euo pipefail

cd "$CODEX_WORKTREE_PATH"

MAIN_WORKTREE="/Users/tianren/coding/cypherair-main"

if [ ! -d "$MAIN_WORKTREE/PgpMobile.xcframework" ]; then
  echo "error: source PgpMobile.xcframework not found at $MAIN_WORKTREE"
  echo "Run ./build-xcframework.sh --release in the main worktree first."
  exit 1
fi

echo "Copying PgpMobile.xcframework from main worktree..."
rm -rf PgpMobile.xcframework
ditto "$MAIN_WORKTREE/PgpMobile.xcframework" PgpMobile.xcframework

if [ -f "$MAIN_WORKTREE/PgpMobile.arm64e-build-manifest.json" ]; then
  cp "$MAIN_WORKTREE/PgpMobile.arm64e-build-manifest.json" .
fi

git status --short --branch
'''
