/// The sealed root, the unlock chain, the session values, the identity
/// wrapping key, and the workflows: onboarding, unlock, relock, passphrase
/// change, reset, and recovery classification.
public enum Vault {}
