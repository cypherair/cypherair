# Architecture Refactor Current-State Audit

> Status: Audit snapshot.
> Date: 2026-05-14.
> Scope: First-party Swift app code under `Sources/`. Generated UniFFI Swift
> bindings in `Sources/PgpMobile/pgp_mobile.swift` are used only as evidence for
> generated type origins, not classified as hand-maintained architecture debt.
> Purpose: Record current architecture boundary problems that affect
> maintainability, using the architecture refactor goals as audit dimensions
> while grounding findings in current code.
> Audience: CypherAir maintainers, reviewers, and coding agents preparing future
> architecture refactor work.
> Truth sources: Current repository files, targeted `rg` reference checks,
> `docs/ARCHITECTURE_REFACTOR_GOALS.md`,
> `docs/ARCHITECTURE_REFACTOR_TARGET.md`, `docs/ARCHITECTURE.md`,
> `docs/SECURITY.md`, `docs/CONVENTIONS.md`,
> `docs/PERSISTED_STATE_INVENTORY.md`, and
> `docs/DOCUMENTATION_GOVERNANCE.md`.

This document is a point-in-time audit. It is not a target architecture, an
implementation plan, a phase plan, or a canonical current-state replacement for
`ARCHITECTURE.md` or `SECURITY.md`. Current code and canonical current-state
documentation outrank this snapshot whenever they drift.

## Summary

CypherAir has already moved several important workflows toward clearer
ownership, especially newer ScreenModel-backed encrypt/decrypt/sign/verify
screens and the protected Contacts domain. The current codebase still has
material architecture debt around layer boundaries, FFI containment, model
responsibility, Contacts runtime shape, and security workflow placement.

The most persistent problem is not one isolated file. It is that generated
UniFFI types, presentation policy, ProtectedData state, and legacy compatibility
projections are used as ordinary app-facing vocabulary. That makes code harder
to reason about because changes to Rust bindings, Contacts storage, or security
authorization rules can ripple into Models, ScreenModels, UI, and tests.

Representative search snapshot:

- `PgpEngine` appears in app composition, most primary services, Contacts
  helpers, key-management subservices, and tutorial container wiring.
- Generated result and enum types such as `PgpError`, `KeyInfo`,
  `SignatureStatus`, `SignatureVerificationState`, `UserIdSelectorInput`,
  `CertificationKind`, and `KeyProfile` appear above a dedicated adapter
  boundary in Models, Views, ScreenModels, and UI-facing helpers.
- The Contacts runtime still exposes and mutates `[Contact]` while also
  maintaining `ContactsDomainSnapshot` and person-centered summary types.
- Several UI routes and ScreenModels still call concrete services or security
  workflows directly, while other routes already use more focused ScreenModels.

## Classification

| Classification | Meaning | Current treatment |
| --- | --- | --- |
| Boundary Violation | A layer depends on details that should be contained behind another boundary. | Record as architecture debt even when behavior is currently correct. |
| Mixed Responsibility | A type owns multiple concerns such as domain data, presentation, persistence validation, and error mapping. | Record as maintainability risk, not necessarily a functional bug. |
| Runtime Compatibility Debt | Legacy compatibility projection or migration logic remains in ordinary runtime paths. | Record where compatibility behavior is active outside a narrow migration boundary. |
| Security-Sensitive Coupling | App or ScreenModel code coordinates Security or ProtectedData internals directly. | Record separately because future edits cross sensitive review boundaries. |
| Acceptable Composition Exception | Concrete dependency construction is expected, but policy ownership may still be too broad. | Do not classify construction alone as a problem. |

## Inventory

| Area | Representative evidence | Current problem | Risk |
| --- | --- | --- | --- |
| Generated FFI error mapping leaks into Models | `CypherAirError.from(_:)` checks `PgpError`, and `CypherAirError.init(pgpError:)` maps generated error cases in [Sources/Models/CypherAirError.swift:136](../Sources/Models/CypherAirError.swift#L136) and [Sources/Models/CypherAirError.swift:144](../Sources/Models/CypherAirError.swift#L144). | The app error vocabulary is app-owned, but generated-error normalization lives in the Models layer. This couples every app-level error consumer to the UniFFI error enum shape. | Medium. Generated error changes can force model and UI-facing updates rather than being contained at an adapter boundary. |
| Generated signature types are model state | `SignatureVerification` stores `SignatureStatus` in [Sources/Models/SignatureVerification.swift:95](../Sources/Models/SignatureVerification.swift#L95). `DetailedSignatureVerification` stores `SignatureStatus`, accepts `SignatureVerificationState`, and maps `DetailedSignatureEntry` / `DetailedSignatureStatus` in [Sources/Models/DetailedSignatureVerification.swift:52](../Sources/Models/DetailedSignatureVerification.swift#L52), [Sources/Models/DetailedSignatureVerification.swift:95](../Sources/Models/DetailedSignatureVerification.swift#L95), and [Sources/Models/DetailedSignatureVerification.swift:203](../Sources/Models/DetailedSignatureVerification.swift#L203). | App-owned verification models are still partly wrappers around generated result records and enums. FFI result structure therefore leaks into service returns, ScreenModel state, detailed UI rendering, and tests. | High. Verification state affects user trust decisions, so generated/API churn risks broad behavioral drift. |
| Generated selector inputs leak through app-owned selector models | `UserIdSelectionOption.selectorInput` constructs a `UserIdSelectorInput` directly in [Sources/Models/UserIdSelectionOption.swift:23](../Sources/Models/UserIdSelectionOption.swift#L23). `CertificateSignatureService.validatedSelectorInput(...)` returns that generated type in [Sources/Services/CertificateSignatureService.swift:298](../Sources/Services/CertificateSignatureService.swift#L298). | `UserIdSelectionOption` is intended to be app-owned selector metadata, but it also owns conversion into a UniFFI selector record. The mapping is not isolated to an FFI call-site adapter. | Medium. Duplicate-User-ID correctness depends on exact selector semantics, so generated selector knowledge should be narrow. |
| Generated profile and certification enums are persisted/app-facing model vocabulary | `PGPKeyIdentity.profile` is `KeyProfile` in [Sources/Models/PGPKeyIdentity.swift:20](../Sources/Models/PGPKeyIdentity.swift#L20). `ContactKeyRecord.profile` is `KeyProfile` in [Sources/Models/Contacts/ContactKeyRecord.swift:13](../Sources/Models/Contacts/ContactKeyRecord.swift#L13), and `ContactKeySummary.profile` carries the same generated enum into UI-facing summaries in [Sources/Models/Contacts/ContactKeySummary.swift:13](../Sources/Models/Contacts/ContactKeySummary.swift#L13). `KeyProfile` receives app-side Codable conformance in [Sources/Extensions/KeyProfile+Codable.swift:6](../Sources/Extensions/KeyProfile+Codable.swift#L6). `ContactCertificationArtifactReference.certificationKind` is `CertificationKind?` in [Sources/Models/Contacts/ContactCertificationArtifactReference.swift:98](../Sources/Models/Contacts/ContactCertificationArtifactReference.swift#L98), and that generated enum receives Codable conformance in [Sources/Models/Contacts/ContactCertificationArtifactReference.swift:285](../Sources/Models/Contacts/ContactCertificationArtifactReference.swift#L285). | Persisted payloads and UI-facing summaries store generated enums directly. This makes generated binding shape part of long-lived app data contracts instead of an adapter concern. | High. Persisted schema compatibility can become tied to generated Swift enum names and cases. |
| FFI calls are distributed across services instead of contained | `AppContainer.makePgpServiceGraph(...)` injects one `PgpEngine` into many services in [Sources/App/AppContainer.swift:252](../Sources/App/AppContainer.swift#L252). `DecryptionService` stores `PgpEngine` directly in [Sources/Services/DecryptionService.swift:35](../Sources/Services/DecryptionService.swift#L35), and similar direct ownership exists in `EncryptionService`, `SigningService`, `PasswordMessageService`, `CertificateSignatureService`, `KeyManagementService`, `ContactService`, `QRService`, `SelfTestService`, and key-management subservices. | There is no dedicated FFI adapter layer. Engine calls, generated result handling, off-main wrappers, and generated error mapping are spread across feature services and helper types. | High. Refactoring or replacing generated APIs requires broad edits across services and models. |
| Partial mapper exists but does not define an adapter boundary | `CertificateSelectionCatalogMapper` maps `DiscoveredCertificateSelectors` to app-owned catalog values in [Sources/Services/CertificateSelectionCatalogMapper.swift:4](../Sources/Services/CertificateSelectionCatalogMapper.swift#L4), while `CertificateSelectionCatalogDiscovery.discover(...)` still returns the raw generated record as part of its tuple in [Sources/Services/CertificateSelectionCatalogDiscovery.swift:5](../Sources/Services/CertificateSelectionCatalogDiscovery.swift#L5). | The mapper is useful, but the raw generated selector record still remains part of the helper contract. This is a localized example of mapping without containment. | Medium. It increases the chance that new callers preserve raw generated values for convenience. |
| Error normalization and generated cancellation policy are scattered | `DecryptionService` catches `PgpError` directly in [Sources/Services/DecryptionService.swift:83](../Sources/Services/DecryptionService.swift#L83) and [Sources/Services/DecryptionService.swift:133](../Sources/Services/DecryptionService.swift#L133). `ContactImportPublicCertificateValidator.mapError(_:)` maps `PgpError` in [Sources/Services/ContactImportPublicCertificateValidator.swift:24](../Sources/Services/ContactImportPublicCertificateValidator.swift#L24). Ordinary views inspect `PgpError.OperationCancelled` in [Sources/App/Keys/ImportKeyView.swift:252](../Sources/App/Keys/ImportKeyView.swift#L252), [Sources/App/Keys/KeyGenerationView.swift:253](../Sources/App/Keys/KeyGenerationView.swift#L253), and [Sources/App/Keys/BackupKeyView.swift:257](../Sources/App/Keys/BackupKeyView.swift#L257). ScreenModels repeat the same cancellation check in [Sources/App/Contacts/AddContactScreenModel.swift:288](../Sources/App/Contacts/AddContactScreenModel.swift#L288), [Sources/App/Keys/SelectiveRevocationScreenModel.swift:307](../Sources/App/Keys/SelectiveRevocationScreenModel.swift#L307), [Sources/App/Contacts/ContactCertificateSignaturesScreenModel.swift:663](../Sources/App/Contacts/ContactCertificateSignaturesScreenModel.swift#L663), and [Sources/App/Contacts/ContactCertificationDetailsScreenModel.swift:833](../Sources/App/Contacts/ContactCertificationDetailsScreenModel.swift#L833). | Generated-error interpretation appears in Models, Services, Views, and ScreenModels. Cancellation, corruption, invalid-key, and QR import semantics are not centralized, and some cancellation ignore policy still depends on a generated enum case above the future adapter boundary. | Medium. Different call sites can accidentally diverge in how they translate generated failures or decide whether an operation was cancelled. |
| Models contain SwiftUI presentation policy | `ColorTheme` imports SwiftUI and returns `Color` and `ActionColors` in [Sources/Models/ColorTheme.swift:1](../Sources/Models/ColorTheme.swift#L1), [Sources/Models/ColorTheme.swift:78](../Sources/Models/ColorTheme.swift#L78), and [Sources/Models/ColorTheme.swift:101](../Sources/Models/ColorTheme.swift#L101). `SignatureVerification` imports SwiftUI and owns `symbolName`, `statusColor`, and localized status text in [Sources/Models/SignatureVerification.swift:1](../Sources/Models/SignatureVerification.swift#L1), [Sources/Models/SignatureVerification.swift:148](../Sources/Models/SignatureVerification.swift#L148), and [Sources/Models/SignatureVerification.swift:160](../Sources/Models/SignatureVerification.swift#L160). | Core Models mix app data with display strings, SF Symbols, and SwiftUI color choices. The Models layer is therefore not a pure domain/payload layer. | Medium. Moving models into a lower-level module or testing them without UI frameworks becomes harder. |
| Models contain localized UI text | `IdentityPresentation.displayName(from:)` returns a localized fallback in [Sources/Models/IdentityPresentation.swift:31](../Sources/Models/IdentityPresentation.swift#L31). `KeyProfile+App` adds display names, descriptions, and security labels to generated `KeyProfile` in [Sources/Models/KeyProfile+App.swift:5](../Sources/Models/KeyProfile+App.swift#L5). `ContactsAvailability` owns localized unavailable titles and descriptions in [Sources/Models/Contacts/ContactsAvailability.swift:45](../Sources/Models/Contacts/ContactsAvailability.swift#L45). | Localized display policy is spread through Models instead of being isolated in presentation extensions or ScreenModel-prepared display state. | Low to Medium. The behavior is visible and useful today, but it keeps presentation and domain concerns intertwined. |
| Models throw Security / ProtectedData errors | `ContactsDomainSnapshot.validateContract()` throws `ProtectedDataError.invalidEnvelope(...)` in [Sources/Models/Contacts/ContactsDomainSnapshot.swift:26](../Sources/Models/Contacts/ContactsDomainSnapshot.swift#L26). `ContactCertificationArtifactReference.validatePayload()` throws `ProtectedDataError.invalidEnvelope(...)` in [Sources/Models/Contacts/ContactCertificationArtifactReference.swift:189](../Sources/Models/Contacts/ContactCertificationArtifactReference.swift#L189). `ContactsPostAuthGateResult` stores `ProtectedDataPostUnlockOutcome` and `ProtectedDataFrameworkState` in [Sources/Models/Contacts/ContactsAvailability.swift:80](../Sources/Models/Contacts/ContactsAvailability.swift#L80). | Contacts domain models know ProtectedData error vocabulary and post-unlock framework state. This embeds storage/security framework details into app-owned domain types. | Medium. Future storage boundary changes can force model changes and broaden security-sensitive review scope. |
| Legacy `Contact` projection remains active runtime state | `ContactService` stores `private var contacts: [Contact] = []` in [Sources/Services/ContactService.swift:27](../Sources/Services/ContactService.swift#L27) while also storing `runtimeSnapshot: ContactsDomainSnapshot?` in [Sources/Services/ContactService.swift:40](../Sources/Services/ContactService.swift#L40). Protected-domain open calls `applyProtectedRuntimeSnapshot(...)`, which projects the snapshot back into `[Contact]` in [Sources/Services/ContactService.swift:1184](../Sources/Services/ContactService.swift#L1184). | The protected Contacts domain is present, but the ordinary runtime still maintains a flat compatibility projection. The legacy projection is not only old-install migration input. | High. Two runtime representations create drift risk and make it harder to know which model is authoritative at a call site. |
| Contacts compatibility mapper is in ordinary mutation paths | Protected add-contact returns `Contact` by calling `compatibilityContact(...)` in [Sources/Services/ContactService.swift:316](../Sources/Services/ContactService.swift#L316). `compatibilityContact(...)` rebuilds projected contacts from the snapshot in [Sources/Services/ContactService.swift:1062](../Sources/Services/ContactService.swift#L1062). The mapper itself converts snapshots into `[Contact]` in [Sources/Services/ContactsCompatibilityMapper.swift:58](../Sources/Services/ContactsCompatibilityMapper.swift#L58). | Compatibility projection is used to shape normal service results, not only legacy migration reads. This keeps legacy `Contact` in app-facing APIs such as `AddContactResult` and import workflows. | High. It slows migration to person-centered Contacts types and keeps older key-replacement semantics visible. |
| ContactService owns too many Contacts responsibilities | `ContactService` constructs legacy repository and migration source in [Sources/Services/ContactService.swift:60](../Sources/Services/ContactService.swift#L60), opens the protected domain in [Sources/Services/ContactService.swift:72](../Sources/Services/ContactService.swift#L72), supports legacy compatibility open in [Sources/Services/ContactService.swift:130](../Sources/Services/ContactService.swift#L130), performs imports in [Sources/Services/ContactService.swift:184](../Sources/Services/ContactService.swift#L184), exposes summaries in [Sources/Services/ContactService.swift:492](../Sources/Services/ContactService.swift#L492), and performs snapshot persistence in [Sources/Services/ContactService.swift:1051](../Sources/Services/ContactService.swift#L1051). | The facade owns availability, migration fallback, legacy projection, import/update, protected mutation rollback, search/tag summaries, certification artifacts, and persistence coordination. | High. The file is a high-coupling center where migration, runtime, and current product behavior are hard to separate. |
| UI / ScreenModel adoption is uneven | Newer flows use ScreenModels, for example `DecryptView` constructs `DecryptScreenModel` in [Sources/App/Decrypt/DecryptView.swift:119](../Sources/App/Decrypt/DecryptView.swift#L119). Other views still orchestrate services directly: `KeyGenerationView.generate()` calls the captured key-management service in [Sources/App/Keys/KeyGenerationView.swift:196](../Sources/App/Keys/KeyGenerationView.swift#L196), `BackupKeyView.exportBackup()` calls `exportKeyBackupData(...)` in [Sources/App/Keys/BackupKeyView.swift:194](../Sources/App/Keys/BackupKeyView.swift#L194), `QRDisplayView.generateQR()` calls `qrService.generateQRCode(...)` in [Sources/App/Contacts/QRDisplayView.swift:68](../Sources/App/Contacts/QRDisplayView.swift#L68), and `ContactDetailView` mutates Contacts directly in [Sources/App/Contacts/ContactDetailView.swift:385](../Sources/App/Contacts/ContactDetailView.swift#L385). | Some routes already follow a ScreenModel pattern, but others still keep async workflow, service calls, mutation handling, and error presentation inside views. | Medium. Behavior remains localized, but repeated view-level orchestration makes future workflow changes harder to test consistently. |
| ScreenModels expose concrete service and generated/security vocabulary | `DecryptScreenModel` action types expose `DecryptionService.Phase1Result`, `FileProgressReporter`, and `AppTemporaryArtifact` in [Sources/App/Decrypt/DecryptScreenModel.swift:6](../Sources/App/Decrypt/DecryptScreenModel.swift#L6). Contact certificate ScreenModels expose `CertificationKind` in action signatures and state in [Sources/App/Contacts/ContactCertificateSignaturesScreenModel.swift:39](../Sources/App/Contacts/ContactCertificateSignaturesScreenModel.swift#L39) and [Sources/App/Contacts/ContactCertificationDetailsScreenModel.swift:40](../Sources/App/Contacts/ContactCertificationDetailsScreenModel.swift#L40), and they expose `CertificateSignatureVerification` state in [Sources/App/Contacts/ContactCertificateSignaturesScreenModel.swift:81](../Sources/App/Contacts/ContactCertificateSignaturesScreenModel.swift#L81) and [Sources/App/Contacts/ContactCertificationDetailsScreenModel.swift:96](../Sources/App/Contacts/ContactCertificationDetailsScreenModel.swift#L96). | ScreenModels improve UI separation, but some public state and action signatures still carry service internals, generated FFI enums, and file/progress infrastructure. | Medium. Tests and views remain coupled to implementation details that could otherwise be hidden behind app-owned request/result models. |
| Services are injected as observable UI environment dependencies | Primary services such as `EncryptionService`, `DecryptionService`, `SigningService`, `KeyManagementService`, `ContactService`, `QRService`, and `SelfTestService` are marked `@Observable`. Views read services from environment, for example [Sources/App/Encrypt/EncryptView.swift:106](../Sources/App/Encrypt/EncryptView.swift#L106), [Sources/App/Contacts/AddContactView.swift:38](../Sources/App/Contacts/AddContactView.swift#L38), and [Sources/App/Settings/SelfTestView.swift:6](../Sources/App/Settings/SelfTestView.swift#L6). | Services act as both business workflow owners and observable UI dependencies. This keeps direct service access natural even on screens that would benefit from ScreenModel-only interaction. | Medium. It blurs service and presentation state ownership and makes it harder to enforce view-thinness consistently. |
| Security workflows surface into Settings ScreenModel | `SettingsScreenModel` stores `AuthenticationManager` and `KeyManagementService` directly in [Sources/App/Settings/SettingsScreenModel.swift:16](../Sources/App/Settings/SettingsScreenModel.swift#L16). It calls `authManager.switchMode(...)` through its default action in [Sources/App/Settings/SettingsScreenModel.swift:66](../Sources/App/Settings/SettingsScreenModel.swift#L66) and creates/uses `LAContext` for local reset authorization in [Sources/App/Settings/SettingsScreenModel.swift:367](../Sources/App/Settings/SettingsScreenModel.swift#L367). | App-facing settings state coordinates authentication mode and local reset authorization directly rather than routing through a narrower service-level workflow. | High. Authentication mode and reset are sensitive boundaries; app-layer orchestration increases review surface. |
| Reset-all workflow lives in App/Settings but owns Security and ProtectedData details | `LocalDataResetService` is under `Sources/App/Settings/` but stores `KeychainManageable`, `ProtectedDataStorageRoot`, `AuthenticationManager`, `ProtectedDataSessionCoordinator`, `AppSessionOrchestrator`, `KeyManagementService`, and `ContactService` in [Sources/App/Settings/LocalDataResetService.swift:21](../Sources/App/Settings/LocalDataResetService.swift#L21). | Local reset is a product workflow, but the implementation directly enumerates and deletes low-level Keychain, ProtectedData, Contacts, defaults, and temporary-artifact surfaces from the App layer. | High. It is intentionally powerful and security-sensitive, but placement makes App code a direct owner of storage/security internals. |
| Protected settings access coordinator owns ProtectedData authorization details | `ProtectedSettingsAccessCoordinator.Dependencies` includes authorization handoff checks, root-key access, domain opening, mutation recovery, and reset closures in [Sources/App/Settings/ProtectedSettingsAccessCoordinator.swift:20](../Sources/App/Settings/ProtectedSettingsAccessCoordinator.swift#L20). It stores and invalidates `LAContext` in [Sources/App/Settings/ProtectedSettingsAccessCoordinator.swift:47](../Sources/App/Settings/ProtectedSettingsAccessCoordinator.swift#L47). | The coordinator is a useful App-facing bridge, but it still models ProtectedData authorization and mutation recovery mechanics in the App layer. | Medium to High. Future ProtectedData policy changes will likely require App-layer changes. |
| AppContainer does more than wiring in post-auth flow | `AppContainer` constructs concrete dependencies, which is expected. Its post-authentication handler also bootstraps first domain state, opens registered ProtectedData domains, opens Contacts, appends migration warnings, loads ordinary settings, updates private-key control state, and invokes recovery checks in [Sources/App/AppContainer.swift:490](../Sources/App/AppContainer.swift#L490). | App composition is the correct place to assemble dependencies, but this closure owns significant sequencing and policy around ProtectedData, Contacts, settings, and private-key recovery. | Medium. Startup and post-unlock behavior is difficult to reason about because policy is embedded in composition code. |
| Tutorial container repeats concrete service/engine wiring | `TutorialSandboxContainer` constructs its own `PgpEngine`, security mocks, `KeyManagementService`, `ContactService`, and PGP services in [Sources/App/Onboarding/TutorialSandboxContainer.swift:48](../Sources/App/Onboarding/TutorialSandboxContainer.swift#L48) and [Sources/App/Onboarding/TutorialSandboxContainer.swift:91](../Sources/App/Onboarding/TutorialSandboxContainer.swift#L91). | The tutorial sandbox legitimately needs isolated composition, but it duplicates knowledge of concrete service construction and direct engine injection. | Low to Medium. It can drift from production wiring unless shared composition boundaries become clearer. |

## Non-Findings And Boundaries

- This audit does not identify a direct UI call to `PgpEngine` in ordinary route
  views. The main FFI issue is direct engine ownership in services/helpers and
  generated types leaking into Models, ScreenModels, and UI-facing contracts.
- `AppContainer` and `TutorialSandboxContainer` are allowed to know concrete
  implementation types as composition roots. The concern is policy breadth and
  duplicated construction knowledge, not the existence of composition code.
- The protected Contacts domain exists and is documented as implemented in
  current-state docs. The audit finding is that ordinary runtime still relies on
  the legacy `Contact` compatibility projection.
- This audit did not attempt to re-validate cryptographic correctness, AEAD
  hard-fail behavior, zero network access, or release packaging. Those remain
  governed by `SECURITY.md`, `TESTING.md`, and existing tests.

## Validation Notes

The audit was prepared from targeted source inspection and searches focused on:

- generated UniFFI symbols above the service boundary
- generated error and cancellation handling above the adapter boundary
- direct `PgpEngine` ownership
- SwiftUI and presentation policy inside `Sources/Models`
- `Contact` / `[Contact]` runtime references
- direct service calls from views
- Security and ProtectedData types inside App and ScreenModel code

Because this is a documentation-only snapshot, Rust and Xcode test runs are not
required unless later changes also touch code, generated files, project files,
entitlements, release metadata, or build settings.
