# PRD Revision Log

> Extracted from [PRD.md](PRD.md) Appendix B for readability.

| Version | Changes |
|---------|---------|
| v4.4 | **Rust/FFI canonical-doc consolidation.** The durable Rust / FFI contract rules and current capability-family ownership summary were migrated into the canonical PRD, TDD, ARCHITECTURE, TESTING, and SECURITY docs, while the temporary Rust/FFI rollout/adoption docs were archived under `docs/archive/`. `PRD.md` and `TDD.md` advanced to v4.4, testing docs now treat the family-level Rust/FFI coverage sections as the canonical validation minima, and stale references to the nonexistent `MacUISmokeTests.swift` path were replaced with current macOS smoke/routing coverage guidance. |
| v4.3 | **Native visionOS documentation sync.** Active entry docs and canonical current-state specs now describe shipped native `visionOS 26.4+` support alongside iOS, iPadOS, and macOS. Rust build matrices were expanded across the doc stack to include `aarch64-apple-visionos` and `aarch64-apple-visionos-sim`, testing docs now document the real `xcodebuild build -scheme CypherAir -destination 'generic/platform=visionOS' CODE_SIGNING_ALLOWED=NO` validation probe and the current absence of a dedicated visionOS XCTest plan, and wording that incorrectly implied iOS-only current-state behavior was corrected in architecture, security, conventions, and Liquid Glass guidance. Archived the superseded `VISIONOS_NATIVE_ADAPTATION_PLAN.md` planning document under `docs/archive/`. |
| v4.2 | **Documentation governance and current-state sync.** Entry docs and canonical specs now describe the shipped direct static-archive Xcode linkage (`libpgp_mobile.a` + `bindings/module.modulemap`) instead of an XCFramework-first runtime story. Test-plan references were aligned across entry docs to include `CypherAir-MacUITests` as targeted macOS UI coverage. Architecture and technical-design docs were updated for Rust helper layout, decrypt phase-1 recipient matching ownership, current storage/defaults, contact metadata, startup temp cleanup, and the true scope of `DiskSpaceChecker`. Rust/FFI companion docs were refreshed so shipped service ownership, detailed-result adoption, and selective revocation are no longer described as pending. Added `DOCUMENTATION_GOVERNANCE.md` and archived superseded audit/review snapshots under `docs/archive/`. |
| v4.1 | **Documentation and infrastructure sync.** Platform baseline updated across active docs to iOS 26.4+ / iPadOS 26.4+ / macOS 26.4+. Architecture docs updated to reflect `AppContainer`, `AppStartupCoordinator`, shared operation helpers, identity presentation helpers, and security recovery stores/coordinator. Security docs updated to describe retryable vs unrecoverable crash-recovery outcomes and generic startup diagnostics. Testing docs updated to document current macOS-local validation flow and the temporary GitHub Actions hosted-runner mismatch (hosted macOS 26.3 vs project target 26.4). |
| v4.0 | **Roadmap update for delivered features.** v1.1 (streaming file processing, dynamic file size limit, progress reporting and cancellation) marked as completed. macOS 26.2+ support delivered and moved from v2.0 to v1.2 (completed). Platform updated to iOS 26.2+ / iPadOS 26.2+ / macOS 26.2+ across PRD header, Section 1.3, Section 1.4. "No macOS" exclusion removed. Build targets updated in CLAUDE.md and TDD.md to include `aarch64-apple-darwin`. v2.0 scope revised: Share Extension + post-quantum cryptography + interop test-pack. |
| v3.9 | **Second cross-version audit against v3.5 docx.** Scenario 11 restored Secure Enclave hardware binding + biometric-only access control description (present in v3.5 Scenario 7, lost during v3.6 rewrite). Section 5.1 restored "badge" in "backup status badge" (UI element specificity). Section 6.1 restored general "⚠️ Possible risk" label alongside format downgrade example (v3.8 had replaced the general label with a single specific case). |
| v3.8 | **Cross-version audit and completeness pass.** Restored content inadvertently lost during v3.6/v3.7 revisions, verified against v3.5 docx and companion documents (SECURITY.md, ARCHITECTURE.md, CLAUDE.md). Changes: Section 4.1 restored "recommended" hint for email field. Section 4.6 added revocation cert export path. Section 8.3 restored "URL scheme requires confirmation" acceptance criterion. Section 9 restored full FFI pipeline description ("Wrapper crate → Swift bindings → XCFramework") and full MIE description ("Hardware Memory Tagging protects vendored OpenSSL C code on A19+ devices"). Section 10.1 MVP checklist restored multi-key, expiry modification, and revocation cert features. |
| v3.7 | **Content restoration and consolidation.** Restored content that was inadvertently removed or over-compressed during v3.6 revision: compression algorithm setting (Disabled) in both profile tables, file output format (.gpg default), full Authentication Mode description (High Security Mode rationale, activation safeguards, technical detail) with cross-references retained as supplementary, detailed App Protection acceptance criteria (High Security biometric-only enforcement), MIE explanatory note on compatibility, Technical Architecture summary (SE wrapping detail, UI framework, storage architecture). Scenarios 5–10 restored with full descriptions (previously marked "unchanged from v3.5" without content). Scenario numbering updated (v3.5 Scenario 7 merged with new Scenario 11). |
| v3.6 | **Dual profile system introduced.** Profile A (Universal Compatible): v4 keys, Ed25519+X25519, SEIPDv1, Iterated+Salted S2K — full GnuPG compatibility. Profile B (Advanced Security): v6 keys, Ed448+X448, SEIPDv2 AEAD OCB, Argon2id — RFC 9580 ecosystem only. Auto format selection by recipient key version. Mixed recipient handling (v4+v6 → SEIPDv1). Profile selection in key generation UX. Acceptance criteria split by profile. Self-test covers both profiles. Scenarios updated for mixed-profile communication. Documents split into PRD + TDD + POC markdown files in repository. |
| v2.0–v3.5 | See previous history. |
