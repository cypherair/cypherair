fn main() {
    // Intentionally empty. UniFFI scaffolding is generated via proc-macros
    // (#[derive(uniffi::...)] and uniffi::setup_scaffolding!() in lib.rs),
    // not via UDL files, so no build-time code generation is needed here.
    // This file exists because Cargo expects build.rs when [build-dependencies]
    // are declared.
}
